import { PageShell } from "@/components/shared/page-shell";

export default function WorkflowsPage() {
  return (
    <PageShell title="Workflows" description="Automate repetitive collection operations with rule-based workflows">
      <div className="flex items-center justify-center h-64 rounded-lg border border-dashed border-border">
        <p className="text-sm text-muted-foreground">Workflows coming soon</p>
      </div>
    </PageShell>
  );
}
