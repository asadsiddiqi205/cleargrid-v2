import { PageShell } from "@/components/shared/page-shell";

export default function LenderConfigPage() {
  return (
    <PageShell title="Lender Configurations" description="Configure lender settings and integrations">
      <div className="flex items-center justify-center h-64 rounded-lg border border-dashed border-border">
        <p className="text-sm text-muted-foreground">Lender Configurations coming soon</p>
      </div>
    </PageShell>
  );
}
